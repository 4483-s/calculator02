// import "./styles.css"
import { append, calcAndDisplay, clearAll } from "./display.js";
document.body.addEventListener("keydown", (e) => {
  if (e.key === "Enter") {
    console.log("hello");
  }
});
